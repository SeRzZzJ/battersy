[package]
name = "battersy"
version = "1.2.0"
edition = "2024"
authors = ["SeRzZzJ <serzzzj.dev@gmail.com>"]
description = "A lightweight, zero-dependency Linux terminal utility that parses battery status using a pipeline architectural pattern."
repository = "https://github.com"
readme = "README.md"
license = "MIT"
keywords = ["battery", "cli", "linux", "status", "terminal"]
categories = ["command-line-utilities", "os::linux-apis"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[lints.rust]
warnings = "deny"

[lints.clippy]
pedantic = "warn"
nursery = "warn"
cargo = "warn"

[dependencies]
